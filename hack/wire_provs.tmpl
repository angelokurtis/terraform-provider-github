// Code generated. DO NOT EDIT.

//go:build wireinject

package main

import (
	"github.com/google/wire"

{{- range .Providers }}
    {{- if ne .PkgPath "github-personal/angelokurtis/terraform-provider-github/cmd/provider" }}
		{{ .PkgName }} {{ .PkgPath | quote }}
	{{- end }}
{{- end }}
)

// providers groups together all constructors needed to build the
// application's core dependencies. It tells Wire how to instantiate key
// components so they can be injected wherever required.
var providers = wire.NewSet(
{{- range $i, $prov := .Providers }}
	{{- if ne $prov.PkgName "main" }}
		{{- $first := printf "%c" (index $prov.Name 0) }}
		{{- if eq $first (upper $first) }}
			{{ $prov.PkgName }}.{{ $prov.Name }},
		{{- end }}
	{{- end }}
{{- end }}
)
